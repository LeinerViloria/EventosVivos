# Integración continua: backend y frontend se analizan en paralelo (jobs independientes).
# El PR queda en verde ("All checks passed") solo cuando ambos jobs terminan correctamente.
#
# Configuración requerida en el repositorio de GitHub (Settings → Secrets and variables → Actions → Secrets):
#     SONAR_TOKEN                 Token de SonarCloud (SonarQube Cloud).
#     SONAR_ORGANIZATION          Organización en SonarCloud.
#     SONAR_PROJECT_KEY_BACKEND   Project key del proyecto backend en SonarCloud.
#     SONAR_PROJECT_KEY_FRONTEND  Project key del proyecto frontend en SonarCloud.
name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  backend:
    name: Backend CI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Verify formatting
        working-directory: src/backend
        run: dotnet format EventosVivos.sln --verify-no-changes

      - name: Setup Java (requerido por SonarScanner)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install SonarScanner for .NET
        run: dotnet tool install --global dotnet-sonarscanner

      - name: Build, test and analyze
        working-directory: src/backend
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          export PATH="$PATH:$HOME/.dotnet/tools"
          dotnet sonarscanner begin \
            /k:"${{ secrets.SONAR_PROJECT_KEY_BACKEND }}" \
            /o:"${{ secrets.SONAR_ORGANIZATION }}" \
            /d:sonar.host.url="https://sonarcloud.io" \
            /d:sonar.token="$SONAR_TOKEN" \
            /d:sonar.cs.opencover.reportsPaths="**/coverage.opencover.xml" \
            /d:sonar.coverage.exclusions="**/RabbitMqEventBus.cs,**/RabbitMqEventStreamConsumer.cs,**/OutboxPublisherService.cs,**/ReservationExpirationService.cs"
          dotnet build EventosVivos.sln -c Release
          dotnet test EventosVivos.sln -c Release --no-build \
            --settings coverlet.runsettings \
            --collect:"XPlat Code Coverage" \
            --results-directory ./TestResults
          dotnet sonarscanner end /d:sonar.token="$SONAR_TOKEN"

  frontend:
    name: Frontend CI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Enable pnpm
        run: corepack enable

      - name: Install dependencies
        working-directory: src/frontend
        run: pnpm install --frozen-lockfile

      - name: Verify formatting
        working-directory: src/frontend
        run: pnpm exec prettier --check .

      - name: Test with coverage
        working-directory: src/frontend
        run: pnpm exec ng test --watch=false --coverage --coverage-reporters=lcovonly

      - name: SonarCloud scan
        uses: SonarSource/sonarqube-scan-action@v5
        with:
          projectBaseDir: src/frontend
          args: >
            -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY_FRONTEND }}
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: https://sonarcloud.io
